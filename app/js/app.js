console.log('this is to test that the app connects to the js file properly', 'Hello World!');

var _build = {
    date : '_update._build.date',
    time : '_update._build.time'
}

console.log(_build);